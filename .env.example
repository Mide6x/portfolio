# Example frontend env (Vite)
# Never commit real secrets; anything prefixed with VITE_ is exposed to the browser.

# Optional: GitHub token for stats (consider removing and using a backend proxy instead)
VITE_GITHUB_TOKEN=

# Admin password should NOT be a VITE_ var in production (it becomes public).
VITE_ADMIN_PASSWORD=
